{
  "name": "meeting-transcript-processing-v2",
  "description": "LangGraph workflow for meeting transcript theme extraction and documentation (Step 1 scaffold)",
  "entrypoint": "agent.py:app",
  "version": "0.1.0",
  "config": {
    "defaults": {
      "configurable": {
        "temperature": 0.2,
        "provider": "google",
        "model": "gemini-2.5-pro"
      }
    }
  }
}
